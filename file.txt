这是一个新文件，需要用commit -a命令吗？
第二行的内容在这里，且是中文。中文在github里可正常显示，但git Bash命令行无法添加中文消息。

这里是第4行，正在学习git rebase命令。再怎么说又修改了这个文件，看看git status会显示什么。
这是第五行，也是当前在学习git rebase命令，可以同步远程，也可以在本地分支间变基。
file.txt删掉了第3行，第6行的情况如图所示。
